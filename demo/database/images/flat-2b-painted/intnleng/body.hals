this uses the same basic script as flat 2a, but with a different
scaling and more cells.

the connected region is identified using gimp fill (so it's physically
connected, rather than a single line).

again, the aim was to produce an interesting monochrome texture.
